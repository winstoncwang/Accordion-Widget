import React, { useState, useEffect } from 'react';

const Convert = ({ language, input }) => {
	useEffect(
		() => {
			console.log(text);
		},
		[ language, input ]
	);

	return <div />;
};

export default Convert;
